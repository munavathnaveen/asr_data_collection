# FastAPI / CORS
CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Hugging Face
HF_TOKEN=hf_xxx
HF_DATASET_REPO=your-username/your-dataset

# Paths (prefer /data if you attach a Persistent Disk)
AUDIO_DIR=/tmp/audio_chunks
DATASET_DIR=/tmp/dataset
